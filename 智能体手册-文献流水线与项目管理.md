# 智能体操作手册：文献获取、格式转换、入库与项目管理

## 1. 项目边界与数据流

项目由上游文献获取子目录和下游 RAG 建库目录组成：

```text
E:\writing-rag\直肠癌文献爬取
  PubMed NBIB → 索引信息.csv → tasks.sqlite → pdfs_merged\PMID_*.pdf
                                                │
                                                │ 按 PMID 增量同步
                                                ▼
E:\writing-rag
  papers\PDF → converted\*.md → Chroma(papers) → converted\已入库\*.md
```

- `直肠癌文献爬取\`：文献检索、下载任务状态和 PDF 主库，现已纳入当前项目；智能体可以在用户授权范围内维护其脚本、配置、队列和报告。
- 下游 `E:\writing-rag`：PDF 转换、元数据映射、向量入库和检索；上游子目录也属于同一项目树。所有手册、脚本修订和 RAG 维护均限制在此目录。
- `mucinous/`、`.claude/tmp/analyze.py`、`sync_mucinous.py`、`final_check.py` 是既往 MAC 阶段遗留物，不用于扩展新的统一流水线。
- 项目不是 Git 仓库。涉及 SQLite、Chroma、tracker、manifest 的修改前，应先制作带时间戳的备份，并保留到校验完成。

## 2. 权威数据与状态文件

| 环节 | 权威文件或目录 | 含义 |
|---|---|---|
| 检索原始记录 | `E:\writing-rag\直肠癌文献爬取\pubmed-RectalNeop-set.nbib` | 9171 条 PubMed/MEDLINE 记录；原始输入不要改写 |
| 下载元数据 | `E:\writing-rag\直肠癌文献爬取\索引信息.csv` | 19 列元数据及下载状态 |
| 下载当前状态 | `E:\writing-rag\直肠癌文献爬取\tasks.sqlite` | `tasks` 为当前状态，`run_history` 为批次历史 |
| 下载历史 | `E:\writing-rag\直肠癌文献爬取\reports\run_history.csv` | 合并任务库时的权威历史源 |
| PDF 主库 | `E:\writing-rag\直肠癌文献爬取\pdfs_merged` | 规范命名为 `PMID_<pmid>.pdf` |
| 下游元数据旧副本 | `papers\索引信息.csv` | 仅保留兼容性，不作为新入库元数据源 |
| 待转换文件 | `papers\` | 可递归放 PDF/DOCX；PMID 批次放顶层 |
| 转换状态 | `.claude\conversion-tracker.json` | 转换成功/失败记录；不要手工编辑 |
| 待入库 Markdown | `converted\*.md` | 仅顶层文件会被独立建库脚本扫描 |
| 已入库原文 | `converted\已入库\*.md` | 已完成入库的 Markdown 归档 |
| 向量库 | `index\chroma_db` | Chroma collection 名为 `papers` |
| 入库清单 | `index\manifest.json` | 按 Chroma source 重建的 SHA-256 增量清单 |
| 元数据映射 | `.claude\tmp\mapping.json`、`mapping_pmid.json` | 前者为既有专题语料，后者由 CSV 生成 |
| 嵌入模型 | `.model-cache\BAAI__bge-m3` | 本地 BAAI/bge-m3；建库和检索均依赖 |

## 3. 文献检索与获取

### 3.1 当前上游范围

截至 2026-08-24，上游状态为：

- PubMed 记录 9171 条，年份 1949–2026；
- 2000 年前 1582 条为 `archived`，当前不下载；
- `done` 2419、`pending` 4074、`not_found` 1075、`failed` 21；
- `pdfs_merged` 有 2422 个有效 PDF，其中 `PMID_11111101`、`PMID_11111102`、`PMID_11111103` 是测试占位文件，不得进入 RAG；
- 当前主策略是优先处理有 PMC 标识的开放获取记录，再处理其他记录。

只使用合法、已获授权或开放获取的全文来源，并遵守来源站点条款。下载镜像的可用性和合规性由执行者按当时环境确认。

### 3.2 不要重新初始化现有状态

以下操作会覆盖或重置状态，不得作为日常续跑命令：

- `scripts\01_parse_nbib.py` 会从 NBIB 重写 `索引信息.csv`，所有条目初始状态均为 `pending`；
- `scripts\02_build_queue.py --fresh` 会重建任务表；
- 直接对 `tasks.sqlite` 批量改状态会改变待处理范围。

只有在用户明确要求重建下载工程、且已备份 `tasks.sqlite`、`索引信息.csv` 和 `reports\run_history.csv` 时，才可执行这些操作。

### 3.3 继续 PMC 批次

在 PowerShell 中：

```powershell
Set-Location 'E:\writing-rag\直肠癌文献爬取'

# 从 b6 开始按每批 400 篇生成任务库
python scripts\12_build_pmc_batches.py --batch-size 400 --max-batches 0 --first-batch 6

# 将 N 替换为实际批次号
python scripts\03_downloader.py --db tasks_pmc_bN.sqlite --pdf-dir pdfs_merged

# 每批完成后严格按顺序同步状态
python scripts\13_sync_batch.py --run pmc_bN --db tasks_pmc_bN.sqlite
python scripts\11_merge_sqlite.py
python scripts\05_report.py --pdf-dir pdfs_merged
python scripts\14_refresh_corpus_meta.py --batch-label pmc_bN-YYYYMMDD
```

批次结束后检查：

- `reports\run_history.csv` 中存在且只存在一组对应的 `Run=pmc_bN` 记录；
- `tasks.sqlite` 的状态分布与本批结果一致；
- `reports\summary.txt` 的 `done` 数与 PDF 校验数一致；
- `pdfs_merged\语料元数据.csv` 包含新增 PMID，且校验为“有效”。

`scripts\11_merge_sqlite.py` 的自动清理名单目前只硬编码到 `pmc_b5`。`b6` 及之后的批次库不会自动删除；不要把“文件仍存在”误判为合并失败，也不要在 `13 → 11 → 05 → 14` 全链校验前清理批次库。

### 3.4 下载状态含义

| 状态 | 含义 | 处理原则 |
|---|---|---|
| `pending` | 未处理或已明确重置待重试 | 下载器只领取此状态 |
| `downloading` | worker 正在处理 | 超过 15 分钟会在下次运行时回收到 `pending` |
| `done` | PDF 已下载 | 仍需通过 `%PDF-` 文件头与最小 30 KB 校验 |
| `not_found` | 当前通道未找到全文 | 可在来源索引更新后按批次重试 |
| `failed` | 网络、挑战或文件校验失败 | 先检查错误与来源状态，再决定重试 |
| `archived` | 当前范围外的 2000 年前记录 | 未经用户确认不得解除封存 |

## 4. 从文献爬取子项目增量同步到 RAG

### 4.1 判定“新文件”

不能把 `直肠癌文献爬取\pdfs_merged` 全量复制到 `papers`。转换后的 Markdown 已移到 `converted\已入库`，如果重新复制原 PDF，转换器可能再次转换同一文献。

新文件必须同时满足：

1. 上游文件名符合 `PMID_<数字>.pdf`；
2. 不是三个测试 PMID；
3. 同名 stem 不存在于下游 `papers`、`converted` 顶层、`converted\已入库` 或 `flash-failed`；
4. 文件头为 `%PDF-` 且大小不低于 30 KB。

先只列候选，不复制：

```powershell
$ragSeen = @{}
Get-ChildItem 'E:\writing-rag\papers' -Recurse -File -Filter '*.pdf' |
  ForEach-Object { $ragSeen[$_.BaseName] = $true }
Get-ChildItem 'E:\writing-rag\converted' -File -Filter '*.md' |
  ForEach-Object { $ragSeen[$_.BaseName] = $true }
Get-ChildItem 'E:\writing-rag\converted\已入库' -File -Filter '*.md' |
  ForEach-Object { $ragSeen[$_.BaseName] = $true }
Get-ChildItem 'E:\writing-rag\flash-failed' -Recurse -File -Filter '*.pdf' |
  ForEach-Object { $ragSeen[$_.BaseName] = $true }

function Test-RagPdfHeader([string]$ragPath) {
  $ragStream = [System.IO.File]::OpenRead($ragPath)
  try {
    $ragHeader = New-Object byte[] 5
    if ($ragStream.Read($ragHeader, 0, 5) -ne 5) { return $false }
    return [System.Text.Encoding]::ASCII.GetString($ragHeader) -eq '%PDF-'
  }
  finally { $ragStream.Dispose() }
}

$ragCandidates = Get-ChildItem 'E:\writing-rag\直肠癌文献爬取\pdfs_merged' -File -Filter 'PMID_*.pdf' |
  Where-Object {
    -not $ragSeen.ContainsKey($_.BaseName) -and
    $_.BaseName -notin @('PMID_11111101','PMID_11111102','PMID_11111103') -and
    $_.Length -ge 30720 -and
    (Test-RagPdfHeader $_.FullName)
  }

$ragCandidates | Select-Object Name, Length, LastWriteTime
```

确认候选数和名称后再复制 PDF；爬取子项目的 `索引信息.csv` 保留为唯一元数据源，不再复制到 `papers`：

```powershell
$ragCandidates | Copy-Item -Destination 'E:\writing-rag\papers'
```

爬取子项目保持不变。不要移动或删除其 `pdfs_merged` 中的文件。

### 4.2 入库前刷新元数据映射

新 PDF 到位后，`gen_pmid_mapping.py` 会直接读取 `直肠癌文献爬取\索引信息.csv`；在 `E:\writing-rag` 执行：

```powershell
Set-Location 'E:\writing-rag'
$env:PYTHONIOENCODING = 'utf-8'
.\.venv\Scripts\python.exe .claude\tmp\gen_pmid_mapping.py
```

要求：

- `unmatched by CSV` 应为 0；
- `entries` 应覆盖 `papers`、`converted` 顶层和 `converted\已入库` 中全部 `PMID_*` stem；
- `mac` 只是按标题中的 `mucin/mucus/黏液` 生成的信息性标签，不是完整的医学分类。`papers\索引信息.csv` 是旧副本，不作为新入库的元数据源。

## 5. PDF/DOCX 转 Markdown

### 5.1 环境与预检

- 固定使用 `E:\writing-rag\.venv\Scripts\python.exe`；
- PowerShell 会话设置 `$env:PYTHONIOENCODING='utf-8'`；
- `mineru-open-api` 必须在 `PATH` 中；当前由用户级 npm 命令提供；
- 超过 10 MB 的文件和 flash 失败后的兜底转换需要环境变量 `MINERU_TOKEN`；不要把令牌写入手册、脚本、日志或版本化文件；
- 不转换或删除 `.zotero-*` 元数据文件；输出始终保持在 `converted` 顶层，不建立嵌套输出目录。

```powershell
Set-Location 'E:\writing-rag'
$env:PYTHONIOENCODING = 'utf-8'
Get-Command mineru-open-api
.\.venv\Scripts\python.exe .claude\batch_convert.py --dry-run
```

转换模式由脚本自动选择：

- PDF 不大于 10 MB：先用 `flash-extract`；失败时若有令牌，再用 `extract` 重试一次；
- PDF 大于 10 MB：直接用 `extract`；
- 成功输出为 `converted\<源文件 stem>.md`；
- 最终失败的源文件移入 `flash-failed\<原父目录名>\`，并写入 tracker。

### 5.2 推荐的一体化运行

当 `converted` 顶层为空，且 `papers` 中只含真正新增的源文件时：

```powershell
.\.venv\Scripts\python.exe .claude\batch_convert.py `
  --ingest --ingest-live-only --delete-source
```

含义：转换成功后立即排队嵌入；入库成功的 Markdown 移到 `converted\已入库`；源 PDF 只在转换成功后删除。上游 `pdfs_merged` 主副本不受影响。

当 `converted` 顶层已有尚未入库的 Markdown 时，去掉 `--ingest-live-only`，允许启动时预扫描：

```powershell
.\.venv\Scripts\python.exe .claude\batch_convert.py --ingest --delete-source
```

短时运行或试跑：

```powershell
# 完成当前文件后，在达到时间上限时停止
.\.venv\Scripts\python.exe .claude\batch_convert.py `
  --ingest --ingest-live-only --delete-source --max-run-minutes 60

# 只测试少量文件
.\.venv\Scripts\python.exe .claude\batch_convert.py `
  --ingest --ingest-live-only --delete-source --convert-limit 3 --ingest-limit 3
```

不要并行启动两个转换/入库进程。每个进程都会在启动时读取一份 manifest，重叠运行可能导致最后写入者覆盖另一进程的清单状态。

### 5.3 用时估算

以现有实测为准：

- 小文件批，中位约 0.7 MB：37–46 秒/篇；
- 较大文件批，中位约 1.4 MB：约 52 秒/篇；
- 入库-only：约 34 秒/篇；
- 大于 10 MB 的 extract 文件按普通文件约 2 倍估算；
- 总时长再增加约 10% 的兜底重试余量。

估算式：`分钟 ≈ 文件数 × 对应秒数 ÷ 60 × 1.1`。

### 5.4 失败处理

- 转换失败不会阻断其余文件；检查控制台、`.claude\tmp\*.log` 和 tracker 中的失败原因。
- 把失败 PDF 移回 `papers` 并不足以重试，因为 tracker 仍会将原路径识别为失败。
- 不手工修改 `.claude\conversion-tracker.json`。如需重试，应先明确失败原因，再把“安全重置单条失败记录”作为独立维护任务实现并校验。
- 入库失败时，转换完成的 Markdown 通常保留在 `converted` 顶层，可在修复环境后执行 `--ingest-only`。

## 6. 向量入库

### 6.1 默认参数

- 模型：`BAAI/bge-m3`，优先读取本地 `.model-cache\BAAI__bge-m3`；
- 数据库：`index\chroma_db`；
- collection：`papers`；
- 切块：按 Markdown 1–4 级标题分段，目标不超过约 800 tokens，段落重叠约 80 tokens，小于 16 tokens 的噪声块丢弃；
- 向量：归一化 embedding，Chroma cosine 距离；
- 元数据：`title/year/journal/doi/authors/mac/folder/evidence/source/section/chunk/supp`。

### 6.2 仅入库顶层 Markdown

```powershell
$env:PYTHONIOENCODING = 'utf-8'
.\.venv\Scripts\python.exe .claude\batch_convert.py --ingest-only
```

该路径会在入库成功后把文件移动到 `converted\已入库`，适合当前项目的统一流水线。

也可直接执行：

```powershell
.\.venv\Scripts\python.exe index\build_index.py
```

但 `build_index.py` 只扫描 `converted\*.md`，不会扫描 `converted\已入库`，也不会在成功后归档 Markdown。当前语料已经归档，因此 `--force` 并不能对全库做真正的重建；不要把它当作现有全库恢复命令。

## 7. 入库后验证

### 7.1 最小验证

```powershell
# 顶层应没有已完成入库的 Markdown
Get-ChildItem 'E:\writing-rag\converted' -File -Filter '*.md'

# 检索冒烟测试
.\.venv\Scripts\python.exe index\retrieve.py `
  'rectal cancer MRI lymph node metastasis' -k 3 --json --device cpu
```

检索结果至少应包含 `score`、`source`、`title`、`year`、`journal`、`doi`、`section` 和 `snippet`，且结果内容与查询主题相符。

### 7.2 完整性口径

完成状态不能只看单一文件：

1. `converted\已入库`：Markdown 归档数量；
2. Chroma：实际 chunk 数和唯一 `source` 数；
3. `manifest.json`：增量 SHA-256 记录；
4. `mapping_pmid.json`：PMID 元数据覆盖；
5. `papers` 与 `flash-failed`：仍待转换和已失败的源文件。

截至 2026-08-28 的审阅快照：

| 项目 | 数量 |
|---|---:|
| 上游真实 PMID PDF | 2419 |
| 下游已归档或仍待转换的 PMID | 2418 |
| 下游待转换 PMID PDF | 454 |
| 已归档 PMID Markdown | 1964 |
| 已归档非 PMID Markdown | 85 |
| Chroma chunks | 56156 |
| Chroma 唯一 source | 2042 |
| `manifest.json` 登记 source / chunks | 2042 / 56156 |
| `flash-failed` | 1：`PMID_31567929.pdf` |

当前 `manifest.json` 已与 Chroma 实库按 source、chunk 数和 mac 元数据一致。历史上重叠入库进程曾造成清单落后；后续必须串行入库。若再次出现漂移，先停止其他写入进程，再执行：

```powershell
Set-Location 'E:\writing-rag'
$env:PYTHONIOENCODING = 'utf-8'
.\.venv\Scripts\python.exe index\reconcile_manifest.py --dry-run
.\.venv\Scripts\python.exe index\reconcile_manifest.py
```

归档目录比 Chroma 多 7 个文件名，原因是 5 组完全相同的 Markdown 内容共享基于内容哈希生成的 chunk ID，Chroma 只保留其中一个 source。三篇 `PMID_33926780`、`PMID_33971571`、`PMID_34466668` 的转换内容均为无关的同一篇文章，应视为语料质量问题；其余为同文献或同内容副本。未经专门的修复、重建和回归检索任务，不要直接删除或重写现有库。

## 8. 项目维护规则

### 8.1 必须遵守

- 只修改 `E:\writing-rag`；`直肠癌文献爬取\` 已是当前项目的受管子目录，允许在用户授权范围内维护文献检索与下载功能。
- 使用 `.venv\Scripts\python.exe` 运行下游 Python，并设置 UTF-8 输出。
- 新 PMID 入库前先同步 CSV，再生成 `mapping_pmid.json`。
- 每个 Chroma 库同一时间只允许一个写入进程。
- 入库后如需校准清单，使用 `index\reconcile_manifest.py`；该工具以 Chroma 实库为准并原子写回 manifest。
- 保持 `converted` 顶层平铺；`converted\已入库` 只保存完成入库的 Markdown。
- 保留已知的 16 篇无 PDF 记录、Melis 2010 重复源和 4 个既有 orphan conversion；不要重复报告或擅自处理。
- CSV 的 Zotero storage key 来自 `File Attachments` 路径中的八字符目录名，不等于 CSV 的 `Key` 列。
- 从用户 Word 文档抽取参考文献时，取最后一个连续编号块。

### 8.2 禁止事项

- 不手工编辑 tracker、Chroma SQLite 或 HNSW 文件；
- 不运行多个并发 ingestor；
- 不把全部上游 PDF 无差别复制回 `papers`；
- 不把 `mucinous/` 当作当前入库入口；
- 不将 `mac` 标签当作病理学金标准；
- 不在令牌、API key、邮箱或访问凭据未知时自行编造配置；
- 不删除源 PDF、批次库、归档 Markdown 或向量库，除非用户明确要求且已完成备份与目标核对。

### 8.3 常见问题

| 现象 | 优先检查 | 处理 |
|---|---|---|
| MinerU 命令找不到 | `Get-Command mineru-open-api` | 修复 PATH；不要改转换逻辑 |
| 大文件立即失败 | `MINERU_TOKEN` 是否存在 | 在当前会话安全设置令牌后重试维护流程 |
| 新文献元数据为 unknown | CSV 是否已同步、mapping 是否在入库前生成 | 重新生成 mapping；需要重写已有 metadata 时另立重建任务 |
| `--ingest-only` 显示无文件 | `converted` 顶层是否为空 | 已归档文件不会被预扫描 |
| manifest 数量小于 Chroma | 是否有重叠入库历史 | 停止其他写入进程，先 dry-run 再执行 `index\reconcile_manifest.py` |
| 同一 PDF 再次被转换 | 是否把已归档文献重新复制到 `papers`，或入库时未删除下游源 PDF | 按四目录 stem 集合做增量同步 |
| 检索首次启动慢 | bge-m3 首次加载 | 同一进程复用 `rag_core` 的模型缓存 |
