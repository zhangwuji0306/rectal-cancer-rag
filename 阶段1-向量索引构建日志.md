# 阶段 1：向量索引构建 — 流程日志

- 项目：`E:\writing-rag`（MAC 文献 RAG 知识库）
- 日期：2026-08-14 晚 — 08-15 凌晨
- 执行决策（用户确认）：本地嵌入模型；回答生成 LLM = DeepSeek（阶段 2）；**全量语料**（converted/ 82 篇，含非 MAC 与 ESGAR，因写作可能涉及其他方向论文）；先做阶段 1（检索索引）。

---

## 一、环境准备

| 项 | 结果 |
|---|---|
| Python | 3.12.0（系统），新建 venv：`E:\writing-rag\.venv` |
| 硬件 | i5-1135G7（4C/8T，2.4GHz）、16GB RAM、MX450 2GB（WDDM） |
| 依赖 | sentence-transformers 5.7.0 + chromadb 1.5.9（2026-08-14 装成） |

### 网络实测（关键）
- PyPI 官方 CDN：~30 KB/s（不可用）→ **改用阿里云镜像** `https://mirrors.aliyun.com/pypi/simple/`（实测 1.58 MB/s，90 秒装完 torch 等大头）
- huggingface.co：直连超时被墙；hf-mirror.com：单连接 ~0.3–1 MB/s，**并发连接被硬限流**（4 并行 → 各 20 B/s），ModelScope 不通
- 阿里云 `pytorch-wheels/cu124` 目录是普通 HTML 目录列表（非 PEP 503 simple index）→ **pip 无法解析**，只能直接 curl 下载 wheel 文件再本地安装

---

## 二、bge-m3 模型获取（2.27GB，已就绪）

位置：`.model-cache/BAAI__bge-m3/`（自包含，运行时零网络）

过程与坑：
1. `snapshot_download` 失败：仓库内 `imgs/.DS_Store` 等垃圾文件 403 → 改用**逐文件 curl**（仅拉必需 11 个小文件 + pytorch_model.bin）
2. 下载中断两次：首次无 `--max-time` 连接假死 → 改为「`--max-time 180 --retry 5 -C -` 循环续传」；恢复后新连接提速至 ~5.7 MB/s，共 2,271,145,830 字节（与 content-length 精确一致）
3. **basename 覆盖 bug**：批量 curl 用 `$(basename $f)` 落盘，`1_Pooling/config.json` 覆盖了根 `config.json` → 修复：重新拉真实模型 config（model_type=xlm-roberta，max_position_embeddings=8194），Pooling 配置归位到 `1_Pooling/`，补建 `2_Normalize/config.json`（仓库缺失）
4. 验证：加载 3.5s，1024 维、规范化、中英相似度 0.438（合理）

---

## 三、脚本（`index/`）

### `build_index.py`
- 输入：`converted/*.md` 全量 82 篇；输出：`index/chroma_db/`（collection `papers`，cosine 空间）+ `index/manifest.json`
- 分块：按 Markdown 标题层级切分（1–4 级），~800 token/块、重叠 80、噪声碎片 <16 token 丢弃；token 数用真实 bge-m3 tokenizer 统计
- 元数据：从 `.claude/tmp/mapping.json` 映射 title/year/journal/doi/authors/mac；别名表关联 ESGAR PART I/II（7TE8XYFI/FZQ7B3VD）、zrac039→Enblad、cancers-1559433→Bong、NIHMS2115008→Javed-Tayyab（按补充材料内容指纹识别）
- 增量：文件 sha256 未变则跳过；`--force` 全量重建；`--device cpu|cuda|auto`（cuda 时 fp16）
- 修复记录：超长段落硬切后仍超限（2271 token）→ 按字符/token 比迭代截断（实测 max ≤795）；ESGAR 刊头字符画碎片（3 token）仍会漏过过滤（sentencepiece 合并空格字母），单条无害

### `retrieve.py`
- `python index/retrieve.py "query" [-k 8] [--mac-only] [--json]`，输出得分/来源/标题/年份/期刊/DOI/章节/片段

### 分块规模
- 全量统计（tokenizer 预跑）：**2519 chunks**（含 ESGAR 52、综述大文件多）

---

## 四、性能瓶颈与对策（核心）

### 隔离基准（`index/bench.py`）
| 项 | CPU (8线程, fp32) | chroma 写入 |
|---|---|---|
| 20 chunks (984 token/块) | **209.5s → 10.5 s/chunk** | 0.3s（可忽略） |

- 全量 2519 chunks × 10.5s ≈ **7.3 小时** → 不可接受
- 已排除：chroma 写入（0.3s）、批量过小（batch 48 无改善，纯算力瓶颈）

### GPU 路径（MX450 2GB，进行中）
1. torch 2.5.0+cu124（阿里云 wheel 直接下载安装，2.5GB @ ~3MB/s）：✅ 安装成功，`cuda available: True`
2. 障碍：transformers 安全策略（CVE-2025-32434）要求 torch ≥ 2.6 才允许 `torch.load`（.bin 权重加载被拦）
3. 对策：下载 torch 2.6.0+cu124（最小跳跃、满足 CVE 门槛）——**已下载 346MB 时后台任务崩溃中断（0xC0000409），待重下**（预计 ~15 分钟）
4. 预期：MX450 fp16 ≈ 1.5–2.5 chunks/s → 2519 chunks ≈ **20–30 分钟**

### 备选方案（若 GPU 路径失败）
- bge-small-zh-v1.5（24M 参数，95MB 下载，~0.5s/chunk → ~40 分钟），代价：512 token 上限（块改 450 token）+ 512 维，检索质量下降

---

## 五、最终构建与验证结果（2026-08-15 白天，已完成）

### 5.1 GPU torch 安装收尾
- torch 2.6.0+cu124（CVE-2025-32434 修复版，2.51GB）续传下载完成 → 本地 wheel 安装（`--force-reinstall`）
- 验证：`torch 2.6.0+cu124 | cuda: True`（MX450 2GB，fp16 推理）

### 5.2 冒烟测试（--limit 2）
- 2 文件 / 61 chunks / 66s（含模型加载 ~50s）→ GPU fp16 + .bin 权重加载（torch 2.6）通过

### 5.3 全量构建（--force）
- **82 文件 / 2519 chunks / 0 跳过 / 3844s（≈64 min）**，与预估分块数精确一致
- 索引位置：`index/chroma_db`（chroma.sqlite3 ~37.9MB）；manifest：`index/manifest.json`
- 语料构成：mac=yes 70 篇 · mac=no 8 篇 · unknown 4 篇（补充材料/孤儿文件）
- 增量幂等性验证：再次运行 `build_index.py` → `No new files (82 already indexed)`（sha256 匹配全部跳过）

### 5.4 检索验证（retrieve.py，bge-m3 多语言）
| 查询 | 结果 |
|---|---|
| "mucin pool patterns on MRI predicting neoadjuvant treatment response…" | 前 5 条全部直接相关：Miranda 2023、Cao 2019/2020×2、Li 2021 放射组学（score 0.74–0.80） |
| "黏液腺癌 预后 黏液成分占比 与 生存"（中文） | 正确召回 Wang 2025 中华外科杂志中文全文（含 ≥50% 黏液占比 WHO 标准等原文片段） |
| "microsatellite instability…" + `--mac-only` | 全部返回 mac=yes；非 MAC 的 guan 2025 MSI 论文被正确过滤 |
| "ESGAR consensus statement structured MRI reporting rectal cancer" | 正确召回 ESGAR2026 PART I（mac=no），alias 表元数据（标题/期刊/DOI）完整 |
- `--json` 输出验证：title/year/journal/doi/authors/section/snippet 字段齐全

### 5.5 阶段 1 完成状态

| 状态 | 事项 |
|---|---|
| ✅ | venv + 依赖、脚本、bge-m3 模型、分块验证、瓶颈定位（CPU 10.5s/chunk → GPU fp16） |
| ✅ | torch 2.6.0+cu124 安装（续传 2.51GB + 本地 wheel 安装，cuda: True） |
| ✅ | 全量构建 2519 chunks / 82 文件（3844s） |
| ✅ | 检索验证：中英混合查询 × 4 组 + `--mac-only` 过滤 + `--json` 输出 |
| ⏳ | 阶段 2：DeepSeek 回答生成（需 `DEEPSEEK_API_KEY`，未开始） |

### 文档
- `AGENTS.md` / `CLAUDE.md` 已新增「Vector index」与网络限制说明（pip 阿里云、HF hf-mirror、本地模型缓存）
- 环境备注：pip 缓存 `~/AppData/Local/pip/cache` 复用；`index/bench.py`、`index/test_chunking.py` 为调试工具保留

---

*报告更新：2026-08-15 12:50（阶段 1 完成：索引已构建并通过检索验证；阶段 2 待用户提供 DEEPSEEK_API_KEY）*
