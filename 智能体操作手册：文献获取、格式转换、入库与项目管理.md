# 智能体操作手册：文献获取、格式转换、入库与项目管理

> 更新日期：2026-08-29  
> 适用项目：`writing-rag`（本地）/ `rectal-cancer-rag`（GitHub 仓库）  
> 适用范围：`E:\writing-rag` 及其受管子目录 `直肠癌文献爬取\`  
> 本手册是项目维护智能体的当前执行规则。若旧脚本、旧报告或历史说明与本手册冲突，以本手册为准；涉及不可逆修改时仍须先备份并验证。

## 1. 项目目标、边界与总数据流

项目由上游文献获取子系统和下游 RAG 建库子系统组成。下一阶段不再以“尽可能下载 PDF”为核心目标，而以“获得可信、可追溯、许可条件明确、可进入 RAG 的正文”为成功标准。

目标架构：

```text
PubMed PMID / NBIB
        ↓
Canonical metadata（tasks.sqlite）
        ↓
OA resolver
   ├── PMC AWS Article Dataset
   ├── Europe PMC
   ├── Unpaywall
   └── 合法 publisher / repository
        ↓
Content fetch
        ↓
内容校验 + 文献身份校验
        ↓
结构化标准化
   ├── JATS XML → Markdown/JSON
   ├── TXT/HTML → Markdown/JSON
   └── PDF → MinerU → Markdown
        ↓
Chunking → Embedding → Chroma
```

当前实际目录仍包括：

```text
E:\writing-rag\直肠癌文献爬取
  pubmed-RectalNeop-set.nbib
  tasks.sqlite
  索引信息.csv
  pdfs_merged\
  reports\
  scripts\

E:\writing-rag
  papers\
  converted\
  converted\已入库\
  flash-failed\
  index\chroma_db
  index\manifest.json
```

边界规则：

- `直肠癌文献爬取\` 属于当前项目，可维护元数据、全文定位、下载、状态数据库和报告。
- `E:\writing-rag` 负责正文转换、标准化、向量入库和检索。
- `mucinous/`、`.claude/tmp/analyze.py`、`sync_mucinous.py`、`final_check.py` 为既往 MAC 阶段遗留物，不作为新的统一流水线入口。
- 本地目录不是 Git 仓库的替代数据库。SQLite、Chroma、tracker、manifest 等运行状态修改前必须备份。

---

## 2. 权威数据源与单向同步规则

### 2.1 唯一权威状态源

从本次重构起：

```text
tasks.sqlite
=
文献规范元数据 + 当前业务状态的唯一权威数据库
```

`索引信息.csv`、`reports/*.csv`、`pdfs_merged\语料元数据.csv` 均视为 SQLite 的导出物或兼容副本，不再与 SQLite 并列成为长期权威源。

历史 `run_history.csv` 在旧批次系统迁移完成前仍作为 legacy 审计材料保留，但新系统的 source-level 请求历史应进入 SQLite 的 `fetch_attempts` 表。

### 2.2 数据只能按以下方向传播

```text
PubMed / 官方 API
        ↓
tasks.sqlite
        ↓
reports/*.csv
        ↓
RAG metadata export
```

禁止日常维护形成：

```text
tasks.sqlite → CSV → 再反向覆盖 tasks.sqlite
```

只有明确执行数据迁移/import，并完成专门校验时，才允许 CSV 反向写入。

### 2.3 主要文件角色

| 文件/目录 | 新角色 |
|---|---|
| `pubmed-RectalNeop-set.nbib` | 原始检索快照，只读保留，不代表永久最新元数据 |
| `tasks.sqlite` | 唯一规范 metadata/status 数据库 |
| `索引信息.csv` | SQLite 导出/兼容副本，不再是独立权威源 |
| `reports/run_history.csv` | legacy 批次审计材料，迁移期保留 |
| `fetch_attempts` | 新系统逐来源请求审计表 |
| `source_candidates` | 新系统 OA/全文候选来源表 |
| `pdfs_merged\` | 历史及仍需 PDF 路径处理的原始 PDF 主副本 |
| `corpus_raw\` | 新系统建议的 XML/TXT/PDF 原始正文目录 |
| `normalized\` 或 `converted\` | 标准化 Markdown/JSON 文档 |
| `.claude\conversion-tracker.json` | PDF/DOCX 转换状态；不要手工编辑 |
| `index\chroma_db` | Chroma collection `papers` |
| `index\manifest.json` | 向量库增量清单，应与 Chroma 实库一致 |

---

## 3. 当前基线与已知问题

### 3.1 当前上游基线

截至现有 2026-08-24 下载报告：

- PubMed 记录：9171 条，年份 1949–2026；
- 2000 年前：1582 条，当前为 `archived`；
- 旧状态：`done=2419`、`pending=4074`、`not_found=1075`、`failed=21`；
- `pdfs_merged` 曾统计为 2422 个有效 PDF；
- 其中 `PMID_11111101`、`PMID_11111102`、`PMID_11111103` 是测试占位文件，不得作为生产语料进入 RAG。

因此历史“2422 PDF”与“2419 done”的差异不是新增生产全文，而是测试 fixture 混入生产目录。

### 3.2 当前旧下载器的关键缺陷

维护智能体必须知道：旧 `03_downloader.py` 的结果不能直接解释成“全文存在/不存在”。

主要问题：

1. 文献依次走 Europe PMC、Sci-Hub DOI/标题等 route，但最终状态主要受最后一个异常影响；前面来源的网络错误可被最后一个 `NotfoundError` 覆盖。
2. Europe PMC 的 429/5xx/HTML 非 PDF 响应没有完整区分临时错误与永久无全文。
3. `config.json` 中 `network_retries`、`pdf_retries`、`pmc_retries` 与部分代码中的硬编码重试次数存在漂移。
4. `02_build_queue.py` 使用 `INSERT OR IGNORE`，后续 PubMed 新增 DOI/PMCID 时已有任务不会被更新。
5. `06_doi_lookup.py` 的作者姓氏解析与 ESummary 常见作者格式不完全一致，可能降低 DOI 补全命中率。
6. `05_report.py` 通过错误文本前缀粗分原因，无法准确统计真正的网络错误、限流、无 OA、题名误配等。
7. PDF 校验目前主要是 `%PDF-` + 最小文件大小，不能证明该 PDF 就是目标 PMID。
8. `downloading` 超 15 分钟回收到 `pending`，缺乏 lease/heartbeat，长任务可能被重复领取。
9. `12_build_pmc_batches.py` / 多批次 SQLite + CSV 的模式复杂，且存在重复 manifest/header 与硬编码清理范围的维护风险。

### 3.3 当前默认策略变化

**不要继续把 `pmc_b6` 及以后旧下载批次作为默认下一步。**

在新 OA availability 报告完成前，应优先执行：

```text
基线备份
→ schema 迁移
→ PubMed metadata refresh
→ DOI/PMCID reconciliation
→ PMC AWS availability dry-run
→ Europe PMC / Unpaywall OA resolution
→ 输出覆盖率报告
```

只有在完成上述步骤并明确知道缺口后，才决定实际全文获取顺序。

---

## 4. 新的成功定义与状态机

### 4.1 RAG 成功定义

旧定义：

```text
PDF 下载成功 → done
```

新定义：

```text
获得可信、可追溯、许可条件明确、能被 RAG 使用的正文
→ fulltext_ready
```

正文格式可以是：

```text
JATS XML
TXT
结构化 HTML
PDF
```

RAG 内容优先级：

```text
结构化 XML
    >
结构化 TXT/HTML
    >
经文献身份验证的 PDF
    >
仅摘要和元数据
```

PMC 文献原则上优先：

```text
JATS XML → section-aware parser → Markdown/JSON → RAG
```

而不是强制：

```text
PDF → MinerU → Markdown
```

### 4.2 新文献状态

目标状态机：

| 状态 | 含义 | 处理原则 |
|---|---|---|
| `pending` | 尚未完成规范元数据处理 | 进入 metadata refresh |
| `metadata_ready` | PMID/DOI/PMCID/题名/年份等规范信息已确认 | 进入 OA resolution |
| `oa_resolved` | 已知道可以尝试的合法全文来源 | 进入 fetch |
| `fetching` | worker 已获得 lease 正在获取正文 | 维护 heartbeat |
| `fulltext_ready` | 已获得并验证全文 | 进入 normalize/RAG |
| `metadata_only` | 当前无可合法自动获取全文 | 保留摘要/元数据，可定期重查 |
| `retryable_error` | 网络、限流、5xx 等暂时错误 | 按 `next_retry_at` 重试 |
| `excluded` | 撤稿、明确不符合范围或其它排除原因 | 保留理由，不自动重试 |
| `archived` | 当前范围外，例如现有 2000 年前记录 | 未经授权不解除 |

旧 `not_found` / `failed` 仅作为迁移期历史值保留，不再作为新系统核心业务语义。

---

## 5. 错误分类与 retry 规则

禁止让自由文本 `last_error` 独自承担业务语义。新系统至少维护：

```text
last_error_class
last_error_detail
next_retry_at
```

推荐 `error_class`：

```text
timeout
connection_error
rate_limit
server_error
source_not_found
no_oa_fulltext
license_restricted
invalid_pdf
invalid_xml
content_too_small
metadata_mismatch
identifier_mismatch
parser_error
storage_error
unknown
```

HTTP 分类：

```text
429                  → rate_limit       → retryable
500/502/503/504      → server_error     → retryable
timeout              → timeout          → retryable
404                  → source_not_found → 仅表示该 source 无结果
403                  → access/license   → 不等于文章不存在
```

必须牢记：

```text
HTTP 500 ≠ 非 OA
HTTP 503 ≠ not_found
HTML 非 PDF ≠ 文献不存在
某一个 source 404 ≠ 所有来源都没有全文
```

推荐 retry：

```text
网络错误：1h → 6h → 24h
429：严格遵守 Retry-After
5xx：15min → 1h → 6h → 24h
近期文献尚未进入 PMC：3d → 7d → 30d
license_restricted：不要高频重试
```

所有官方 API 应共用统一 `request_with_retry()` / `HttpClient`，支持 connect/read timeout、Retry-After、指数退避、jitter、结构化日志和最大尝试次数。

---

## 6. 新数据库结构要求

### 6.1 `tasks`

保留 `pmid` 主键并逐步扩展：

```text
pmid

doi
pmcid

title
title_norm
year
journal
issn
authors
first_author
first_author_family
pub_type
language

oa_status
license
reuse_allowed

content_status
content_source
content_format
content_path
source_url
content_sha256

metadata_updated_at
source_updated_at
retrieved_at
attempt_count

status
last_error_class
last_error_detail
next_retry_at

worker_id
lease_until
heartbeat_at

retracted
excluded_reason
created_at
updated_at
```

### 6.2 `fetch_attempts`

每尝试一个来源写一条记录：

```text
attempt_id
pmid
source
route
url
identifier
started_at
finished_at
http_status
outcome
error_class
error_detail
retryable
retry_after
content_type
content_length
worker_id
```

文献最终状态由 source-level attempt 综合推导，不能由“最后一次异常”决定。

### 6.3 `source_candidates`

保存 OA/全文候选：

```text
pmid
source
url
format
version
license
is_oa
reuse_allowed
priority
resolved_at
```

来源例：

```text
PMC_AWS
EuropePMC
Unpaywall
Publisher
InstitutionalRepository
```

---

## 7. 文献获取完整工作流

### Workflow 0：安全检查和基线备份

任何 schema migration、批量 metadata 更新或状态重算前：

1. 检查 `tasks.sqlite`、`索引信息.csv`、`reports/run_history.csv`、`pdfs_merged/`；
2. 记录总 PMID、各 status、PDF 数、有效 PDF 数、DOI 数、PMCID 数、全文数；
3. 创建 `backups/YYYYMMDD-HHMMSS/`；
4. 至少备份 `tasks.sqlite`、`索引信息.csv`、`run_history.csv`、`config.json`；
5. 校验备份可打开、PMID 主键无重复、行数一致。

禁止在无备份情况下执行 schema migration。

### Workflow 1：清理测试数据污染

将以下测试文件从生产语料路径移出：

```text
PMID_11111101
PMID_11111102
PMID_11111103
```

建议进入：

```text
tests/fixtures/pdfs/
```

新代码不得在多个脚本中分散硬编码测试 PMID 黑名单；测试数据应通过目录和 fixture 配置隔离。

验收：

```text
生产 PDF 数
=
数据库中 production PDF/fulltext 对应数
```

### Workflow 2：建立最新 PubMed metadata 基线

历史 NBIB 是检索快照，不应永久作为最新 metadata。

输入当前全部 PMID，通过 PubMed 官方批量接口获取最新 XML：

```text
PMID list
  ↓
EPost / batched EFetch
  ↓
PubMed XML
```

至少更新：

```text
PMID
DOI
PMCID
Title
Authors
Journal
ISSN
Publication date
Publication type
Language
Article IDs
```

原则：

```text
NBIB = 原始检索快照
PubMed latest XML = 当前规范 metadata
```

NBIB 不删除、不覆盖。

### Workflow 3：metadata UPSERT

旧 `INSERT OR IGNORE` 必须逐步替换为 metadata UPSERT：

```sql
INSERT ...
ON CONFLICT(pmid) DO UPDATE
```

允许更新：

```text
doi / pmcid / title / year / journal / authors / ...
```

不得因为 metadata refresh 自动覆盖：

```text
已成功 content_path
历史 fetch attempts
retrieved_at
已确认业务状态
```

新 metadata 非空时更新；新字段为空时一般不删除已有可靠 identifier。

验收：已存在 PMID 在 PubMed 后补 PMCID 后能够更新 PMCID，同时保留原全文和尝试历史。

### Workflow 4：OA availability resolution

完成 metadata reconciliation 后再定位全文。

#### 4.1 有 PMCID：PMC AWS 优先

优先检查 PMC Article Dataset，记录可用：

```text
article_version
license
xml_url
text_url
pdf_url
updated_at
```

不得仅凭“存在 PMCID”推断可无限制再利用；必须保存 article-level license / reuse 状态。

#### 4.2 Europe PMC

PMC AWS 没有合适正文时检查 Europe PMC。

优先：

```text
fullTextXML
```

而不是优先 PDF render。

#### 4.3 有 DOI：Unpaywall

记录：

```text
is_oa
oa_status
best_oa_location
url
url_for_pdf
host_type
version
license
```

#### 4.4 DOI 补全

优先级：

```text
1. PubMed 同 PMID ArticleId
2. PubMed metadata
3. OpenAlex
4. Crossref
```

第三方候选必须综合：

```text
Title + Year + First author + Journal/ISSN + Volume/Pages
```

不得只按标题相似度采信。

### Workflow 5：统一作者 family name

修复旧 DOI lookup 中作者格式差异。建立 `normalize_author_family()`，尽可能使用 API 结构化 `family` 字段。

需兼容：

```text
Smith AB
Smith, Andrew B
Andrew B Smith
```

目标统一到：

```text
smith
```

### Workflow 6：正文获取

候选来源优先级：

```text
Priority 1  PMC AWS XML
Priority 2  Europe PMC XML
Priority 3  PMC AWS TXT
Priority 4  合法 OA HTML/XML
Priority 5  合法 OA PDF
```

建议原始正文目录：

```text
corpus_raw/
  PMID_123/
    source.json
    article.xml
  PMID_456/
    source.json
    article.pdf
```

`source.json` 至少保存：

```text
pmid
doi
pmcid
source
url
license
retrieved_at
sha256
format
version
```

### Workflow 7：内容验证

#### XML

必须验证：

```text
XML 可解析
article/title 存在
正文长度达到最低要求
PMCID/PMID 与目标匹配
```

#### PDF

历史 `%PDF-` + 30KB 校验只属于文件级预检。进入 RAG 前还要验证：

```text
PDF parser 可打开
page_count > 0
前 1–2 页可提取文本
题名匹配
DOI 匹配（若存在）
PMID/PMCID 匹配（若可见）
```

建议独立保存：

```text
file_valid
bibliographic_match
```

只有两者均通过才进入生产 RAG。

### Workflow 8：结构化正文标准化

PMC JATS XML 至少识别：

```text
Title
Abstract
Introduction
Methods
Results
Discussion
Conclusion
Tables / Table captions
Figures / Figure captions
Supplementary information
References
```

建议标准化输出：

```text
normalized/PMID_<pmid>.md
```

或：

```text
normalized/PMID_<pmid>.json
```

Markdown 推荐 front matter：

```markdown
---
pmid:
doi:
pmcid:
title:
year:
journal:
source:
license:
---
```

PDF 来源继续复用现有 MinerU 转换链。

### Workflow 9：RAG 入库前校验

正文入库必须满足：

```text
PMID 唯一
metadata match
content SHA256 已计算
非测试 fixture
非重复正文或重复关系已明确
正文长度足够
```

manifest 建议逐步增加：

```text
PMID
content_sha256
metadata_sha256
source
format
embedding_model
chunker_version
indexed_at
```

### Workflow 10：lease 与并发控制

新 fetch worker 使用：

```text
worker_id
lease_until
heartbeat_at
```

领取任务必须事务性完成：

```text
可领取状态
→ fetching
→ lease_until = now + N
```

worker 定期 heartbeat；只有 `lease_until < now` 才允许其它 worker 回收，避免旧 15 分钟固定回收造成重复任务。

### Workflow 11：报告

新报告至少输出：

```text
Corpus target
Metadata ready
DOI coverage
PMCID coverage
OA fulltext resolvable
PMC AWS available
Europe PMC available
Unpaywall available
Fulltext ready
Metadata-only
Retryable
License restricted
Validation failed
Normalized for RAG
Indexed
```

并能按 source/error_class 回答：哪个来源贡献了多少全文、哪个来源失败最多、网络/限流多少、真正无 OA 全文多少、当前 RAG 覆盖率多少。

---

## 8. 批次、旧下载器与 Legacy 迁移规则

### 8.1 旧 `pmc_bN` 流程不再是默认入口

历史命令：

```powershell
python scripts\12_build_pmc_batches.py ...
python scripts\03_downloader.py ...
python scripts\13_sync_batch.py ...
python scripts\11_merge_sqlite.py
python scripts\05_report.py
python scripts\14_refresh_corpus_meta.py ...
```

只作为迁移期历史流程保留。除非用户明确要求验证旧链或新系统还未覆盖某个已授权来源，否则不要自动继续 `pmc_b6`。

### 8.2 旧代码暂不立即删除

保留：

```text
03_downloader.py
altcha_solver.py
selenium_fallback.py
旧 mirror config
```

在新流程经过平行验证后，迁入：

```text
legacy/
```

默认 CLI 不调用 legacy；仅在人工明确指定并确认来源合规时使用。

### 8.3 新 batch 设计

长期不应继续无限扩展：

```text
tasks_pmc_b1.sqlite
tasks_pmc_b2.sqlite
...
```

优先直接用 SQLite 条件领取任务；如必须保留 batch，新增：

```text
batches
batch_members
```

表，而不是建立大量独立数据库和 CSV 状态源。

---

## 9. 配置与外部服务规则

`config.json` 只保存非敏感默认参数。建议逐步统一为：

```json
{
  "pubmed_batch_size": 200,
  "max_workers": 4,
  "connect_timeout": 15,
  "read_timeout": 60,
  "max_retries": 4,
  "backoff_base": 2,
  "backoff_max": 120
}
```

以下通过环境变量提供：

```text
CROSSREF_MAILTO
UNPAYWALL_EMAIL
NCBI_API_KEY
MINERU_TOKEN
```

仓库仅保留 `.env.example`，不得写入真实 API key、token、邮箱或其它访问凭据。

Crossref 仅用于 metadata/identifier resolution，不能直接证明 OA 全文存在。Unpaywall 用于 DOI 对应 OA location。所有第三方 metadata 候选必须通过文献身份校验后写入规范字段。

---

## 10. 推荐代码结构与 CLI

不要继续扩大单体 `03_downloader.py`。目标结构：

```text
scripts/
├── metadata/
│   ├── pubmed.py
│   ├── crossref.py
│   └── identifiers.py
├── sources/
│   ├── pmc_aws.py
│   ├── europepmc.py
│   ├── unpaywall.py
│   └── publisher.py
├── pipeline/
│   ├── resolve.py
│   ├── fetch.py
│   ├── validate.py
│   └── normalize.py
├── db/
│   ├── schema.py
│   ├── migrations.py
│   └── repository.py
└── cli/
    ├── refresh_metadata.py
    ├── resolve_oa.py
    ├── fetch_content.py
    ├── normalize_content.py
    ├── report.py
    └── sync_rag.py
```

目标日常工作流：

```text
01 refresh metadata
↓
02 resolve OA
↓
03 fetch
↓
04 validate
↓
05 normalize
↓
06 sync RAG
↓
07 report
```

每一步必须：幂等、可重复、可断点续跑、不删除成功结果。

---

## 11. 从上游增量同步到现有 PDF RAG 路径

新 XML/JSON 直入 RAG 的统一实现完成前，历史及新增 PDF 仍可使用现有增量同步流程，但必须按 PMID 去重，不能全量复制 `pdfs_merged`。

### 11.1 判定新 PDF

新文件必须：

1. 文件名符合 `PMID_<数字>.pdf`；
2. 不属于测试 fixture；
3. 同名 stem 不存在于 `papers`、`converted` 顶层、`converted\已入库` 或 `flash-failed`；
4. 文件至少通过 `%PDF-` 和大小预检；
5. 新系统启用后还必须通过 bibliographic validation。

现有 PowerShell 候选检查可继续使用：

```powershell
$ragSeen = @{}
Get-ChildItem 'E:\writing-rag\papers' -Recurse -File -Filter '*.pdf' |
  ForEach-Object { $ragSeen[$_.BaseName] = $true }
Get-ChildItem 'E:\writing-rag\converted' -File -Filter '*.md' |
  ForEach-Object { $ragSeen[$_.BaseName] = $true }
Get-ChildItem 'E:\writing-rag\converted\已入库' -File -Filter '*.md' |
  ForEach-Object { $ragSeen[$_.BaseName] = $true }
Get-ChildItem 'E:\writing-rag\flash-failed' -Recurse -File -Filter '*.pdf' |
  ForEach-Object { $ragSeen[$_.BaseName] = $true }

function Test-RagPdfHeader([string]$ragPath) {
  $ragStream = [System.IO.File]::OpenRead($ragPath)
  try {
    $ragHeader = New-Object byte[] 5
    if ($ragStream.Read($ragHeader, 0, 5) -ne 5) { return $false }
    return [System.Text.Encoding]::ASCII.GetString($ragHeader) -eq '%PDF-'
  }
  finally { $ragStream.Dispose() }
}

$ragCandidates = Get-ChildItem 'E:\writing-rag\直肠癌文献爬取\pdfs_merged' -File -Filter 'PMID_*.pdf' |
  Where-Object {
    -not $ragSeen.ContainsKey($_.BaseName) -and
    $_.Length -ge 30720 -and
    (Test-RagPdfHeader $_.FullName)
  }

$ragCandidates | Select-Object Name, Length, LastWriteTime
```

确认后再复制：

```powershell
$ragCandidates | Copy-Item -Destination 'E:\writing-rag\papers'
```

上游原始正文主副本不要因下游转换而删除。

### 11.2 元数据映射

迁移完成前，现有 `gen_pmid_mapping.py` 仍可能读取 `直肠癌文献爬取\索引信息.csv`。因此每次 SQLite metadata 更新后必须先导出/同步 CSV，再生成 mapping；长期目标是让 mapping 直接读取 SQLite 或统一 metadata export。

```powershell
Set-Location 'E:\writing-rag'
$env:PYTHONIOENCODING = 'utf-8'
.\.venv\Scripts\python.exe .claude\tmp\gen_pmid_mapping.py
```

要求：

- `unmatched by CSV` 为 0；
- `entries` 覆盖 `papers`、`converted` 顶层和 `converted\已入库` 中全部 `PMID_*` stem；
- `mac` 仅为信息性标签，不是病理学金标准。

---

## 12. PDF/DOCX 转 Markdown

### 12.1 环境与预检

- 固定使用 `E:\writing-rag\.venv\Scripts\python.exe`；
- PowerShell 设置 `$env:PYTHONIOENCODING='utf-8'`；
- `mineru-open-api` 必须在 `PATH` 中；
- 大文件及 flash 失败兜底需要 `MINERU_TOKEN`；
- 不转换或删除 `.zotero-*` 元数据文件；
- 输出保持在 `converted` 顶层，不建立无必要的嵌套目录。

```powershell
Set-Location 'E:\writing-rag'
$env:PYTHONIOENCODING = 'utf-8'
Get-Command mineru-open-api
.\.venv\Scripts\python.exe .claude\batch_convert.py --dry-run
```

当前模式：

- PDF ≤10 MB：先 `flash-extract`，失败且有 token 时再 `extract`；
- PDF >10 MB：直接 `extract`；
- 成功：`converted\<stem>.md`；
- 最终失败：进入 `flash-failed\<原父目录名>\` 并记录 tracker。

### 12.2 一体化运行

当 `converted` 顶层为空且 `papers` 中只含新增源文件：

```powershell
.\.venv\Scripts\python.exe .claude\batch_convert.py `
  --ingest --ingest-live-only --delete-source
```

若 `converted` 顶层已有待入库 Markdown：

```powershell
.\.venv\Scripts\python.exe .claude\batch_convert.py --ingest --delete-source
```

短时或小样本：

```powershell
.\.venv\Scripts\python.exe .claude\batch_convert.py `
  --ingest --ingest-live-only --delete-source --max-run-minutes 60

.\.venv\Scripts\python.exe .claude\batch_convert.py `
  --ingest --ingest-live-only --delete-source --convert-limit 3 --ingest-limit 3
```

不要并行启动两个转换/入库进程；多个进程可能基于不同 manifest 快照写回，造成清单漂移。

### 12.3 用时估算

现有实测可继续用于人工计划：

- 小文件批中位约 0.7 MB：37–46 秒/篇；
- 中位约 1.4 MB：约 52 秒/篇；
- 入库-only：约 34 秒/篇；
- >10 MB extract 可按普通文件约 2 倍估算；
- 总时长额外增加约 10% 重试余量。

估算：`分钟 ≈ 文件数 × 对应秒数 ÷ 60 × 1.1`。

### 12.4 失败处理

- 转换失败不阻断其余文件；检查控制台、`.claude\tmp\*.log` 和 tracker；
- 把失败 PDF 移回 `papers` 不等于真正重置；tracker 仍可能记录失败；
- 不手工修改 `.claude\conversion-tracker.json`；
- 入库失败时，已生成 Markdown 通常留在 `converted` 顶层，修复环境后用 `--ingest-only`。

---

## 13. 向量入库

### 13.1 默认参数

- 模型：`BAAI/bge-m3`，优先本地 `.model-cache\BAAI__bge-m3`；
- 数据库：`index\chroma_db`；
- collection：`papers`；
- 切块：按 Markdown 1–4 级标题分段，目标 ≤约 800 tokens，重叠约 80 tokens，小于 16 tokens 噪声块丢弃；
- embedding 归一化，Chroma cosine 距离；
- 现有元数据包含 `title/year/journal/doi/authors/mac/folder/evidence/source/section/chunk/supp`。

### 13.2 仅入库顶层 Markdown

```powershell
$env:PYTHONIOENCODING = 'utf-8'
.\.venv\Scripts\python.exe .claude\batch_convert.py --ingest-only
```

入库成功后文件移动至 `converted\已入库`。

也可直接运行：

```powershell
.\.venv\Scripts\python.exe index\build_index.py
```

但它只扫描 `converted\*.md`，不扫描 `converted\已入库`，也不负责归档。不要把 `--force` 误认为对现有全库的完整恢复命令。

---

## 14. 入库后验证

### 14.1 最小验证

```powershell
Get-ChildItem 'E:\writing-rag\converted' -File -Filter '*.md'

.\.venv\Scripts\python.exe index\retrieve.py `
  'rectal cancer MRI lymph node metastasis' -k 3 --json --device cpu
```

检索结果至少包含 `score`、`source`、`title`、`year`、`journal`、`doi`、`section`、`snippet`，内容应与查询主题相符。

### 14.2 完整性口径

不能只看单一文件：

1. `converted\已入库`：Markdown 归档数量；
2. Chroma：实际 chunk 数和唯一 source；
3. `manifest.json`：增量 SHA-256 清单；
4. `mapping_pmid.json`：PMID 元数据覆盖；
5. `papers` / `flash-failed`：待转换与失败源文件；
6. 新系统启用后还要检查 `tasks.fulltext_ready`、`normalized` 与 `fetch_attempts`。

截至 2026-08-28 的历史审阅快照：

| 项目 | 数量 |
|---|---:|
| 上游真实 PMID PDF | 2419 |
| 下游已归档或仍待转换的 PMID | 2418 |
| 下游待转换 PMID PDF | 454 |
| 已归档 PMID Markdown | 1964 |
| 已归档非 PMID Markdown | 85 |
| Chroma chunks | 56156 |
| Chroma 唯一 source | 2042 |
| `manifest.json` source / chunks | 2042 / 56156 |
| `flash-failed` | 1：`PMID_31567929.pdf` |

当前历史 `manifest.json` 已与 Chroma 实库按 source、chunk 数和 mac 元数据一致。若再次漂移：

```powershell
Set-Location 'E:\writing-rag'
$env:PYTHONIOENCODING = 'utf-8'
.\.venv\Scripts\python.exe index\reconcile_manifest.py --dry-run
.\.venv\Scripts\python.exe index\reconcile_manifest.py
```

历史归档与 Chroma 存在少量相同内容共享哈希/错误正文问题；未经专门修复、重建和回归检索任务，不直接删除或重写现有库。

---

## 15. P0–P3 实施计划

### P0：首先完成

1. SQLite 成为唯一权威 metadata/status source；
2. 建立 `fetch_attempts`；
3. 修复 `not_found` / transient error 分类；
4. 正确区分 404、429、5xx、timeout、license、no OA；
5. metadata UPSERT；
6. 接入 PMC AWS Article Dataset；
7. 将三个测试 PDF 移出生产 corpus。

### P1：紧接 P0

1. 批量刷新 PubMed metadata；
2. 接入 Europe PMC XML；
3. 接入 Unpaywall；
4. JATS XML → Markdown；
5. bibliographic validation；
6. 统一 retry client。

### P2

1. 修复 DOI 作者 family name；
2. 重构 batch；
3. lease + heartbeat；
4. 新 report system。

### P3

1. 整理 legacy crawler；
2. 依赖锁定；
3. 单元测试 + integration tests；
4. 清理 Git 中不应版本化的运行态数据库和临时文件。

---

## 16. 第一次实施的严格顺序

首次执行重构时不要一次性修改全部链路：

```text
STEP 1  建立 baseline report
STEP 2  完整备份
STEP 3  新增 schema migration
STEP 4  保持旧 downloader 不动，实现 PubMed metadata refresh
STEP 5  metadata reconciliation，验证 DOI/PMCID
STEP 6  PMC AWS resolver，只做 availability dry-run
STEP 7  统计预计可获取全文数量
STEP 8  抽取 20–50 篇测试 PMC AWS fetch
STEP 9  验证 XML → Markdown
STEP 10 扩大到全部适用 PMCID
STEP 11 接入 Unpaywall
STEP 12 新旧 pipeline 平行验证后再降级旧 crawler
```

### P0 验收

必须同时满足：

```text
所有 PMID 唯一
SQLite 与导出 CSV 不存在 DOI/PMCID 漂移
HTTP 503 不会变成 not_found
HTTP 429 可以按策略 retry
每次 source 请求有 fetch_attempt
测试 PMID 不存在于生产 corpus
PMCID 后补可以通过 UPSERT 更新
PMC AWS availability 可以批量检查
```

### P1 验收

```text
PMC AWS XML 成功率可统计
Europe PMC XML 成功率可统计
Unpaywall OA availability 可统计
XML 无需 PDF/MinerU 即可进入 RAG
PDF 经 bibliographic_match 后才可进入 RAG
```

---

## 17. 周期性维护工作流

### 新增 PubMed 检索结果后

```text
导入新 PMID
→ PubMed metadata refresh
→ UPSERT
→ OA resolution
→ fetch
→ validate
→ normalize
→ RAG
→ report
```

### 定期重查

对以下记录重新检查：

```text
retryable_error
+
近期 metadata_only
```

重点查看：

```text
PMCID 是否新出现
PMC AWS inventory 是否新增
Unpaywall 是否新出现 OA
```

允许状态自动升级：

```text
metadata_only → fulltext_ready
```

---

## 18. 项目维护规则

### 18.1 必须遵守

- 修改 SQLite、Chroma、tracker、manifest 前先备份；
- 使用 `.venv\Scripts\python.exe` 运行下游 Python，并保持 UTF-8；
- SQLite 为上游唯一规范 metadata/status source；
- 在旧 mapping 工具迁移完成前，SQLite 更新后先导出 CSV，再生成 `mapping_pmid.json`；
- 每个 Chroma 库同一时间只允许一个写入进程；
- 入库后需要校准时使用 `index\reconcile_manifest.py`，先 dry-run；
- 保持 `converted` 顶层平铺，`converted\已入库` 只保存已入库 Markdown；
- `mac` 标签只是信息性标签，不是病理学金标准；
- Zotero storage key 来自 `File Attachments` 路径中的八字符目录名，不等于 CSV `Key`；
- 从用户 Word 文档抽取参考文献时取最后一个连续编号块；
- 只使用合法、已授权或开放获取的全文来源，并保存 article-level source/license 信息。

### 18.2 禁止事项

维护智能体不得：

1. 无备份执行 schema migration；
2. 重跑 NBIB 后直接覆盖现有下载/全文状态；
3. 日常使用 `--fresh` 清空任务状态；
4. 把 HTTP 429/5xx 归为文献不存在；
5. 把“有 PMCID”直接等同于“允许无限制再利用”；
6. 把测试 PDF 放进生产 corpus；
7. 把 CSV 当作 SQLite 的平行权威源；
8. 未做 bibliographic validation 就把未知 PDF 进入 RAG；
9. 把验证码绕过/反爬对抗继续作为生产主策略；
10. 在新 pipeline 未完成平行验证前删除旧 crawler；
11. 手工编辑 tracker、Chroma SQLite 或 HNSW 文件；
12. 并发启动多个 ingestor；
13. 把全部上游 PDF 无差别复制回 `papers`；
14. 在凭据未知时编造 token/API key/邮箱；
15. 未经明确授权和备份删除源正文、批次库、归档 Markdown 或向量库。

---

## 19. 常见问题

| 现象 | 优先检查 | 处理 |
|---|---|---|
| Europe PMC/官方源返回 503 | `fetch_attempts.error_class` | 标为 `server_error` 并重试，绝不当 `not_found` |
| API 返回 429 | `Retry-After` | 遵守服务端等待时间，记录 `rate_limit` |
| PMID 后来出现 PMCID | PubMed metadata refresh / UPSERT | 更新 metadata，不重置成功全文和历史 attempt |
| DOI 候选很多 | Title/year/author/journal/ISSN/pages | 多信号验证，不能只看标题 |
| PDF 文件头正常但正文不对 | bibliographic validation | 标 `metadata_mismatch`，不得入库 |
| 新文献元数据为 unknown | SQLite→CSV export、mapping 是否刷新 | 先修 metadata export/mapping，不改向量逻辑 |
| MinerU 命令找不到 | `Get-Command mineru-open-api` | 修复 PATH |
| 大文件立即失败 | `MINERU_TOKEN` | 安全设置环境变量后重试 |
| `--ingest-only` 无文件 | `converted` 顶层 | 已归档文件不会被预扫描 |
| manifest 小于 Chroma | 是否有并发入库历史 | 停止写入，dry-run 后 `reconcile_manifest.py` |
| 同一 PDF 再次转换 | 是否把已见 stem 重放 `papers` | 按四目录 stem 集合做增量同步 |
| fetch 任务被重复领取 | lease/heartbeat 是否实现 | 使用 `lease_until`，不要依赖固定 15 分钟回收 |
| 检索首次启动慢 | bge-m3 首次加载 | 同一进程复用模型缓存 |

---

## 20. 当前维护智能体的立即任务

在没有新的用户指令覆盖本节时，下一阶段优先级如下：

```text
任务 A  备份并生成数据库 baseline
任务 B  设计并迁移新 schema
任务 C  批量刷新全部 PMID 的 PubMed metadata
任务 D  比较更新前后的 DOI/PMCID
任务 E  建立 PMC AWS inventory resolver
任务 F  对有 PMCID 且尚无可信全文的文章做 availability dry-run
任务 G  输出新的 OA coverage report
```

第一份新覆盖率报告至少提供：

```text
目标文献总数
有 DOI 数 / 覆盖率
有 PMCID 数 / 覆盖率
PMC AWS XML available
PMC AWS PDF available
Europe PMC XML available
Unpaywall OA available
metadata-only
retryable
license restricted
identifier 异常
fulltext ready
normalized for RAG
indexed
```

完成该报告后，才决定大规模正文抓取的下一优先级。

项目最终不再以“下载了多少 PDF”作为主要健康指标，而以：

> 目标 PubMed 文献中，有多少获得可信规范元数据；其中多少具有许可明确的可用全文；这些全文中有多少通过身份验证、标准化并成功进入 RAG。

整个系统必须保持：**可追溯、可重试、幂等、可恢复、可审计、许可信息明确、数据源唯一、正文身份可验证。**